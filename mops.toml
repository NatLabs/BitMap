[package]
name = "bit-map"
version = "0.1.2"
description = "A data structure for fast set operations on a set of integers each represented by a bit."
repository = "https://github.com/NatLabs/BitMap"
keywords = ["set", "bitmap", "data-structure"]
license = "MIT"

[dependencies]
base = "0.13.5"
map = "9.0.1"
itertools = "0.2.2"
augmented-btrees = "0.5.2"
vector = "0.4.2"


[dev-dependencies]
test = "2.0.0"
bench = "1.0.0"
fuzz = "0.2.1"

[toolchain]
moc = "0.16.0"
